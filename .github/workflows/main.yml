name: RDP

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
                             -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "SecurityLayer" -Value 0 -Force

          netsh advfirewall firewall delete rule name="RDP-Tailscale" -ErrorAction SilentlyContinue
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389

          Restart-Service -Name TermService -Force

      - name: Create RDP User with Hardcoded Password
        run: |
          $RdpPassword = "Aman@123"
          $securePass = ConvertTo-SecureString $RdpPassword -AsPlainText -Force

          if (Get-LocalUser -Name "RDP" -ErrorAction SilentlyContinue) {
              Remove-LocalUser -Name "RDP" -ErrorAction SilentlyContinue
          }

          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"

          echo "RDP_PASSWORD=$RdpPassword" >> $env:GITHUB_ENV

      - name: Prepare User Folders
        run: |
          $userRoot = "C:\Users\RDP"
          $downloads = "$userRoot\Downloads"

          New-Item -ItemType Directory -Path $userRoot -Force | Out-Null
          New-Item -ItemType Directory -Path $downloads -Force | Out-Null

      - name: Copy & Extract ZIP to Downloads
        run: |
          $source = "$env:GITHUB_WORKSPACE\assets\Apna.zip"
          $downloads = "C:\Users\RDP\Downloads"

          $zipPath = "$downloads\Apna.zip"
          $extractPath = "$downloads\Apna"

          Copy-Item -Path $source -Destination $zipPath -Force

          Expand-Archive -LiteralPath $zipPath -DestinationPath $extractPath -Force

          Write-Host "ZIP extracted to $extractPath"

      - name: Run setup.bat
        run: |
          $downloads = "C:\Users\RDP\Downloads"
          $extractPath = "$downloads\Apna"
          $batFile = "$extractPath\setup.bat"

          if (-Not (Test-Path $batFile)) {
            Write-Error "setup.bat not found at: $batFile"
            exit 1
          }

          Write-Host "Running setup.bat..."

          # Run setup.bat normally
          $process = Start-Process -FilePath $batFile -PassThru
          $process.WaitForExit()

          # Force close if PAUSE exists
          Stop-Process -Id $process.Id -ErrorAction SilentlyContinue

          Write-Host "setup.bat completed successfully."

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Connect to Tailscale
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID

          $tsIP = $null
          $retries = 0
          
          while (-not $tsIP -and $retries -lt 10) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep -Seconds 5
              $retries++
          }

          if (-not $tsIP) {
              Write-Error "Tailscale IP not assigned."
              exit 1
          }

          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Verify RDP
        run: |
          $test = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389
          
          if (-not $test.TcpTestSucceeded) {
              Write-Error "RDP not reachable."
              exit 1
          }

          Write-Host "RDP is active and reachable."

      - name: Keep Runner Alive
        run: |
          Write-Host ""
          Write-Host "=== RDP ACCESS ==="
          Write-Host "IP Address : $env:TAILSCALE_IP"
          Write-Host "Username   : RDP"
          Write-Host "Password   : $env:RDP_PASSWORD"
          Write-Host "=================="
          Write-Host ""

          while ($true) {
              Start-Sleep -Seconds 300
              Write-Host "[$(Get-Date)] RDP still alive..."
          }
